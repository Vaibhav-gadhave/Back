version: '3'
services:
  portainer:
    image: portainer/portainer-ce:2.0.1
    container_name: portainer
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - Portainer:/data
    ports:
      - 8090:9000 
    restart: always

volumes:
  Portainer:
    driver: local

 
